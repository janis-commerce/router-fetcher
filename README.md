# Router-Fetcher

## Response Object

Response of Router

* `RouterResponse`, an `Object` with:
    * `endpoint`
        * type: `String`
        * The endpoint of microservice.
    * `httpMethod`
        * type: `String`
        * The httpMethod of endpoint.
